import { Activity, Building2, PhoneCall, Users } from "lucide-react";
import { MonthlyChart } from "@/components/dashboard/monthly-chart";
import { WeeklyChart } from "@/components/dashboard/weekly-chart";
import { PeriodFilter } from "@/components/dashboard/period-filter";
import { FranchiseeCommunicationTable } from "@/components/dashboard/franchisee-communication-table";
import { prisma } from "@/lib/db";
function parse(value?:string){const d=value?new Date(`${value}T00:00:00`):null;return d&&!Number.isNaN(d.getTime())?d:null}function iso(d:Date){return d.toISOString().slice(0,10)}
export default async function DashboardPage({searchParams}:{searchParams:Promise<{start?:string;end?:string}>}){const q=await searchParams;const now=new Date();const start=parse(q.start)||new Date(now.getFullYear(),now.getMonth(),1);const endDay=parse(q.end)||now;const end=new Date(endDay);end.setHours(23,59,59,999);const where={contactedAt:{gte:start,lte:end}};const [contacts,active]=await Promise.all([prisma.contact.findMany({where,orderBy:{contactedAt:"desc"},select:{type:true,contactedAt:true,franchisee:{select:{id:true,name:true,unitName:true}}}}),prisma.franchisee.count({where:{active:true}})]);const map=new Map<string,{id:string;name:string;unitName:string;whatsapp:number;telefone:number;video:number;presencial:number;total:number;last:Date}>();for(const c of contacts){const item=map.get(c.franchisee.id)||{...c.franchisee,whatsapp:0,telefone:0,video:0,presencial:0,total:0,last:c.contactedAt};if(c.type==="WHATSAPP")item.whatsapp++;if(c.type==="TELEFONE")item.telefone++;if(c.type==="VIDEO_CHAMADA")item.video++;if(c.type==="PRESENCIAL")item.presencial++;item.total++;if(c.contactedAt>item.last)item.last=c.contactedAt;map.set(c.franchisee.id,item)}const rows=[...map.values()].sort((a,b)=>b.total-a.total).map(x=>({...x,last:x.last.toLocaleDateString("pt-BR")}));const count=(type:string)=>contacts.filter(c=>c.type===type).length;const qualified=contacts.filter(c=>c.type!=="WHATSAPP").length;const channelData=[{name:"WhatsApp",value:count("WHATSAPP")},{name:"Telefone",value:count("TELEFONE")},{name:"Vídeo",value:count("VIDEO_CHAMADA")},{name:"Presencial",value:count("PRESENCIAL")}];const days=Math.max(1,Math.ceil((end.getTime()-start.getTime())/86400000)+1);const trend=Array.from({length:days},(_,i)=>{const day=new Date(start);day.setDate(day.getDate()+i);return{name:new Intl.DateTimeFormat("pt-BR",{day:"2-digit",month:"2-digit"}).format(day),value:contacts.filter(c=>c.contactedAt.toDateString()===day.toDateString()).length}});const cards=[{label:"Franqueados ativos",value:active,icon:Building2},{label:"Contatos no período",value:contacts.length,icon:Activity},{label:"Contatos qualificados",value:qualified,icon:PhoneCall},{label:"Franqueados contatados",value:rows.length,icon:Users}];return <div className="space-y-6"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-[var(--brand-primary)]">Visão histórica</p><h1 className="mt-1 text-3xl font-semibold text-[var(--brand-secondary)]">Indicadores da rede</h1><p className="mt-2 text-slate-500">De {start.toLocaleDateString("pt-BR")} até {endDay.toLocaleDateString("pt-BR")}.</p></div><PeriodFilter start={iso(start)} end={iso(endDay)}/><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{cards.map(({label,value,icon:Icon})=><div key={label} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><span className="text-sm text-slate-500">{label}</span><span className="rounded-xl bg-[#eef7ef] p-3 text-[var(--brand-primary)]"><Icon className="h-4 w-4"/></span></div><p className="mt-6 text-4xl font-semibold text-[var(--brand-secondary)]">{value}</p></div>)}</div><div className="grid gap-6 xl:grid-cols-2"><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-lg font-semibold">Canais de comunicação</h2><p className="mt-1 text-sm text-slate-500">Distribuição no período selecionado.</p><MonthlyChart data={channelData}/></section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-lg font-semibold">Evolução de contatos</h2><p className="mt-1 text-sm text-slate-500">Volume diário no período selecionado.</p><WeeklyChart data={trend}/></section></div><FranchiseeCommunicationTable rows={rows}/></div>}
