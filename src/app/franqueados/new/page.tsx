"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { ImagePlus, LoaderCircle } from "lucide-react";
export default function NewFranchiseePage(){const router=useRouter();const [form,setForm]=useState({name:"",unitName:"",photoUrl:"",moment:"IMPLANTACAO" as "IMPLANTACAO"|"INAUGURADA",active:true});const [error,setError]=useState("");const [saving,setSaving]=useState(false);async function upload(file?:File){if(!file)return;setSaving(true);setError("");try{const body=new FormData();body.append("file",file);const response=await fetch("/api/uploads",{method:"POST",body});const payload=await response.json();if(!response.ok)throw new Error(payload.message||"Não foi possível enviar a foto.");setForm(current=>({...current,photoUrl:payload.url}))}catch(reason){setError(reason instanceof Error?reason.message:"Não foi possível enviar a foto.")}finally{setSaving(false)}}async function submit(event:React.FormEvent){event.preventDefault();setSaving(true);setError("");try{const response=await fetch("/api/franchisees",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const payload=await response.json();if(!response.ok)throw new Error(payload.message||"Não foi possível salvar o franqueado.");router.push(`/franqueados/${payload.id}`);router.refresh()}catch(reason){setError(reason instanceof Error?reason.message:"Não foi possível salvar o franqueado.")}finally{setSaving(false)}}return <div className="mx-auto max-w-2xl rounded-3xl border border-slate-200 bg-white p-7 shadow-sm"><p className="text-xs font-bold uppercase tracking-[.2em] text-[var(--brand-primary)]">Novo cadastro</p><h1 className="mt-1 text-3xl font-semibold text-[var(--brand-secondary)]">Adicionar franqueado</h1><p className="mt-2 text-slate-500">Preencha os dados iniciais da nova unidade.</p><form onSubmit={submit} className="mt-7 space-y-5"><label className="flex cursor-pointer items-center gap-4 rounded-2xl border border-dashed border-slate-300 bg-slate-50 p-4 transition hover:border-[var(--brand-primary)]">{form.photoUrl?<img src={form.photoUrl} alt="Prévia" className="h-20 w-20 rounded-xl object-cover"/>:<span className="flex h-20 w-20 items-center justify-center rounded-xl bg-[#eef7ef] text-[var(--brand-primary)]"><ImagePlus className="h-8 w-8"/></span>}<span><b className="block text-[var(--brand-secondary)]">Selecionar foto</b><small className="mt-1 block text-slate-500">JPG, PNG ou WebP — até 5 MB</small></span><input type="file" accept="image/*" className="hidden" onChange={e=>upload(e.target.files?.[0])}/></label><div className="grid gap-5 md:grid-cols-2"><label className="text-sm font-semibold text-slate-700">Nome<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-3 font-normal" required/></label><label className="text-sm font-semibold text-slate-700">Unidade<input value={form.unitName} onChange={e=>setForm({...form,unitName:e.target.value})} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-3 font-normal" required/></label></div><div className="grid gap-5 md:grid-cols-2"><label className="text-sm font-semibold text-slate-700">Momento<select value={form.moment} onChange={e=>setForm({...form,moment:e.target.value as typeof form.moment})} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-3 font-normal"><option value="IMPLANTACAO">Implantação</option><option value="INAUGURADA">Inaugurada</option></select></label><label className="flex items-end gap-2 pb-3 text-sm font-semibold text-slate-700"><input type="checkbox" checked={form.active} onChange={e=>setForm({...form,active:e.target.checked})}/>Unidade ativa</label></div>{error?<p className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</p>:null}<button disabled={saving} className="inline-flex items-center gap-2 rounded-xl bg-[var(--brand-primary)] px-5 py-3 font-semibold text-white disabled:opacity-60">{saving?<><LoaderCircle className="h-4 w-4 animate-spin"/>Salvando...</>:"Cadastrar franqueado"}</button></form></div>}
