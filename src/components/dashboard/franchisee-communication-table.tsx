"use client";
import { useMemo, useState } from "react";
import { Search } from "lucide-react";
type Row={id:string;name:string;unitName:string;whatsapp:number;telefone:number;video:number;presencial:number;total:number;last:string};
export function FranchiseeCommunicationTable({rows}:{rows:Row[]}){const [query,setQuery]=useState("");const visible=useMemo(()=>rows.filter(row=>`${row.name} ${row.unitName}`.toLocaleLowerCase().includes(query.toLocaleLowerCase())),[rows,query]);return <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-wrap items-center justify-between gap-4 border-b border-slate-100 px-6 py-5"><div><h2 className="text-lg font-semibold text-[var(--brand-secondary)]">Comunicação por franqueado</h2><p className="mt-1 text-sm text-slate-500">Resumo de todos os canais no intervalo selecionado.</p></div><span className="rounded-full bg-[#b8ee35]/35 px-3 py-2 text-sm font-bold text-[var(--brand-secondary)]">{visible.length} unidades</span></div><div className="border-b border-slate-100 bg-slate-50/70 px-6 py-4"><label className="flex max-w-md items-center gap-3 rounded-xl border border-slate-200 bg-white px-3 py-2.5 shadow-sm"><Search className="h-4 w-4 text-[var(--brand-primary)]"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Filtrar por nome ou unidade" className="w-full bg-transparent text-sm outline-none"/></label></div><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-[.12em] text-slate-500"><tr><th className="px-6 py-4">Franqueado</th><th className="px-4 py-4">WhatsApp</th><th className="px-4 py-4">Telefone</th><th className="px-4 py-4">Vídeo</th><th className="px-4 py-4">Presencial</th><th className="px-4 py-4">Total</th><th className="px-6 py-4">Último contato</th></tr></thead><tbody className="divide-y divide-slate-100">{visible.map(row=><tr key={row.id} className="hover:bg-[#eef7ef]/50"><td className="px-6 py-4"><b className="text-[var(--brand-secondary)]">{row.name}</b><p className="mt-1 text-xs text-slate-500">{row.unitName}</p></td><td className="px-4 py-4">{row.whatsapp}</td><td className="px-4 py-4">{row.telefone}</td><td className="px-4 py-4">{row.video}</td><td className="px-4 py-4">{row.presencial}</td><td className="px-4 py-4"><b>{row.total}</b></td><td className="px-6 py-4 text-slate-600">{row.last}</td></tr>)}{visible.length===0?<tr><td colSpan={7} className="px-6 py-12 text-center text-slate-500">Nenhum franqueado encontrado.</td></tr>:null}</tbody></table></div></section>}
