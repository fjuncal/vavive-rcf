"use client";
import { useState } from "react";
import { usePathname, useRouter, useSearchParams } from "next/navigation";
import { Check, RotateCcw } from "lucide-react";
function iso(date:Date){return date.toISOString().slice(0,10)}
export function PeriodFilter({start,end}:{start:string;end:string}){const router=useRouter();const path=usePathname();const search=useSearchParams();const [from,setFrom]=useState(start);const [to,setTo]=useState(end);const today=iso(new Date());function range(days:number){const d=new Date();d.setDate(d.getDate()-days+1);return{from:iso(d),to:today}}function isActive(days:number){const r=range(days);return start===r.from&&end===r.to}function apply(a=from,b=to){if(!a||!b||a>b)return;const params=new URLSearchParams(search);params.set("start",a);params.set("end",b);router.push(`${path}?${params}`)}function preset(days:number){const r=range(days);setFrom(r.from);setTo(r.to);apply(r.from,r.to)}const monthStart=iso(new Date(new Date().getFullYear(),new Date().getMonth(),1));const monthActive=start===monthStart&&end===today;const chip=(active:boolean)=>active?"border-[#0b8f45] bg-[#0b8f45] text-white shadow-sm":"border-slate-200 bg-white text-slate-600 hover:border-[#0b8f45] hover:text-[#0b8f45]";return <section className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-[var(--brand-primary)]">Período de análise</p><h2 className="mt-1 text-xl font-semibold text-[var(--brand-secondary)]">Escolha o recorte dos indicadores</h2></div><div className="flex flex-wrap gap-2"><button onClick={()=>preset(7)} className={`rounded-full border px-4 py-2 text-sm font-semibold transition ${chip(isActive(7))}`}>Últimos 7 dias</button><button onClick={()=>preset(30)} className={`rounded-full border px-4 py-2 text-sm font-semibold transition ${chip(isActive(30))}`}>Últimos 30 dias</button><button onClick={()=>{setFrom(monthStart);setTo(today);apply(monthStart,today)}} className={`rounded-full border px-4 py-2 text-sm font-semibold transition ${chip(monthActive)}`}>Este mês</button></div></div><div className="mt-5 grid gap-4 border-t border-slate-100 pt-5 md:grid-cols-[1fr_1fr_auto_auto]"><label className="text-sm font-semibold text-slate-700">Data inicial<input type="date" value={from} onChange={e=>setFrom(e.target.value)} className="mt-2 block w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-3 outline-none focus:border-[var(--brand-primary)]"/></label><label className="text-sm font-semibold text-slate-700">Data final<input type="date" value={to} onChange={e=>setTo(e.target.value)} className="mt-2 block w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-3 outline-none focus:border-[var(--brand-primary)]"/></label><button onClick={()=>apply()} className="mt-auto inline-flex h-12 items-center justify-center gap-2 rounded-xl bg-[var(--brand-primary)] px-5 font-semibold text-white"><Check className="h-4 w-4"/>Atualizar</button><button onClick={()=>router.push(path)} className="mt-auto inline-flex h-12 items-center justify-center gap-2 rounded-xl border border-slate-200 px-4 font-semibold text-slate-600"><RotateCcw className="h-4 w-4"/>Limpar</button></div></section>}
